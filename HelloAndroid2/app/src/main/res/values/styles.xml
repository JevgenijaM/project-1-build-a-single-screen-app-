<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="info_text_style" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Style for information. -->
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">#039BE5</item>
        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:textSize">@dimen/info_text_style</item>
        <item name="android:paddingTop">@dimen/info_text_style_padding_top</item>
    </style>

    <style name="image_one" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Style for first image. -->
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">200dp</item>
        <item name="android:scaleType">centerCrop</item>
    </style>

    <style name="image_two" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Style for second image. -->
        <item name="android:layout_width">100dp</item>
        <item name="android:layout_height">100dp</item>
        <item name="android:scaleType">centerCrop</item>
        <item name="android:layout_centerHorizontal">true</item>
    </style>
</resources>